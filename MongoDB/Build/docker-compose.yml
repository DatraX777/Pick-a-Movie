version: '3'
services:
  mongodb:
    image: mongo:latest
    container_name: 'Mongodb'
    volumes:
      - ./mongo-volume:/data/db
    networks:
      - default
    ports:
      - '27017:27017' 

networks:
  default:
    external: 
      name: your-network